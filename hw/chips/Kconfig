# Models of chips that sit next to a microcontroller rather than inside it,
# shared by the boards that fit them.

config MCP23S08
    bool
    select SSI

config LED_DEMUX
    bool
    select VCD_TRACE

config PARALLEL_SRAM
    bool

config WS2812
    bool

config VCD_TRACE
    bool
